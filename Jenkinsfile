pipeline {
  agent {
    node {
      label 'master'
    }

  }
  stages {
    stage('') {
      steps {
        sh 'echo "Hello world"'
      }
    }

  }
  environment {
    deploy_env = 'app'
  }
}